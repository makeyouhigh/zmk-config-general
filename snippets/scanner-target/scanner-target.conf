## Prospector Scanner
# ===== REQUIRED =====
CONFIG_ZMK_STATUS_ADVERTISEMENT=y
# Shown on scanner (max 8 chars)
CONFIG_ZMK_STATUS_ADV_KEYBOARD_NAME="MyKeyboard"

# ===== POWER OPTIMIZATION (all have sensible defaults) =====
# CONFIG_ZMK_STATUS_ADV_ACTIVITY_BASED=y
# CONFIG_ZMK_STATUS_ADV_ACTIVE_INTERVAL_MS=200
# CONFIG_ZMK_STATUS_ADV_IDLE_INTERVAL_MS=30000
# CONFIG_ZMK_STATUS_ADV_ACTIVITY_TIMEOUT_MS=10000

# ===== SPLIT KEYBOARD =====
# Fetch peripheral battery
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y
# Which side is central (LEFT/RIGHT)
CONFIG_ZMK_STATUS_ADV_CENTRAL_SIDE="LEFT"

# ===== PERIPHERAL BATTERY MAPPING (v2.1, for multi-peripheral setups) =====
# Use when connection order doesn't match physical layout (e.g., after settings_reset)
# Keyboard half slot (default: 0)
# CONFIG_ZMK_STATUS_ADV_HALF_PERIPHERAL=0
# Aux1 slot - e.g., trackball (default: 1)
# CONFIG_ZMK_STATUS_ADV_AUX1_PERIPHERAL=1
# Aux2 slot - e.g., numpad (default: 2)
# CONFIG_ZMK_STATUS_ADV_AUX2_PERIPHERAL=2

# ===== WPM TRACKING =====
# WPM calculation window (5-120s)
CONFIG_ZMK_STATUS_ADV_WPM_WINDOW_SECONDS=30

# ===== CHANNEL (v1.1.2+, optional) =====
# For multi-keyboard environments - filter by channel on scanner side
# 0=broadcast to all (default)
# CONFIG_PROSPECTOR_CHANNEL=0
# 1-9=recommended (touch UI compatible)
# CONFIG_PROSPECTOR_CHANNEL=1